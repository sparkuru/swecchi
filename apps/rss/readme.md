# rss

个人订阅源生成与聚合应用。一个 feed 名称对应一套明确的来源、过滤规则和输出策略，例如 `game` 或 `image`。

## 边界

- 拥有 feed 定义、来源配置、条目标准化和 RSS/Atom 输出。
- 前台读取请求只读取已缓存或已生成的结果；抓取、解析和刷新由后台任务完成。
- 不提供通用网页爬虫接口，也不承担任意第三方源的长期存档。
- 每个 feed 明确标记公开或私有，并在其说明中列出数据源与刷新策略。

详见 [路由说明](routes.md)。
