# shortlinks

个人短链应用。它负责把受控的短码映射到目标 URL，并提供创建、查询、撤销和跳转能力。

## 边界

- 拥有短码、目标 URL、创建者、创建时间、过期时间和启用状态。
- 负责校验目标 URL 与避免短码冲突；跳转结果可以被缓存，短链记录是事实来源。
- 不负责 URL 安全扫描、网页内容抓取或通用分析；这些能力若需要，应由独立应用提供。
- 管理接口默认私有，跳转接口可按单条短链配置为公开。

详见 [路由说明](routes.md)。
