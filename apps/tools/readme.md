# tools

面向个人工作流的小工具集合。它容纳尚不值得拥有独立数据所有权和发布节奏的能力；每个工具由自己的二级路由和文档条目定义。

## 边界

- 无状态、快速完成的操作可同步返回，例如文本哈希。
- 网络访问、文件处理或可能耗时的操作必须通过任务接口异步执行，例如 Telegraph 归档。
- 工具不能以“任意代码执行”或“任意内网请求”的形式暴露；输入目标、下载大小、并发和保留期都需要限制。

详见 [路由说明](routes.md)。
